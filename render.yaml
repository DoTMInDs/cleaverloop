services:
  # -------------------------------------------------------------
  # 1. CleaverLoop AI Web Service (Django / Gunicorn)
  # -------------------------------------------------------------
  - type: web
    name: cleaverloop-web
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 4 --threads 2 --keep-alive 5 --timeout 120"
    plan: starter
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.8
      - key: DJANGO_SETTINGS_MODULE
        value: config.settings.production
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: DATABASE_URL
        fromDatabase:
          name: cleaverloop-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: cleaverloop-redis
          property: connectionString
      - key: STORAGE_BACKEND
        value: s3

  # -------------------------------------------------------------
  # 2. CleaverLoop AI Background Worker (Celery)
  # -------------------------------------------------------------
  - type: worker
    name: cleaverloop-celery-worker
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "celery -A config worker --loglevel=info --concurrency=4 -Q celery,video,image,render"
    plan: starter
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.8
      - key: DJANGO_SETTINGS_MODULE
        value: config.settings.production
      - key: SECRET_KEY
        fromService:
          type: web
          name: cleaverloop-web
          envVarKey: SECRET_KEY
      - key: DATABASE_URL
        fromDatabase:
          name: cleaverloop-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: cleaverloop-redis
          property: connectionString

  # -------------------------------------------------------------
  # 3. CleaverLoop AI Celery Beat (Periodic Tasks / Health Poller)
  # -------------------------------------------------------------
  - type: worker
    name: cleaverloop-celery-beat
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "celery -A config beat --loglevel=info"
    plan: starter
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.8
      - key: DJANGO_SETTINGS_MODULE
        value: config.settings.production
      - key: SECRET_KEY
        fromService:
          type: web
          name: cleaverloop-web
          envVarKey: SECRET_KEY
      - key: DATABASE_URL
        fromDatabase:
          name: cleaverloop-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: cleaverloop-redis
          property: connectionString

# -------------------------------------------------------------
# Databases & Key-Value Stores
# -------------------------------------------------------------
databases:
  - name: cleaverloop-db
    plan: starter
    databaseName: cleaverloop_prod
    user: cleaverloop_user
    postgresMajorVersion: 16

# Render Redis / Key-Value
# Note: On Render, Redis can be provisioned via Dashboard or Render Redis Addon
